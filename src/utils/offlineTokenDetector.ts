// Temporary stub for offline token detector
export const startOfflineTokenDetection = () => {
  console.log('Offline token detection started (stub)');
};

export const stopOfflineTokenDetection = () => {
  console.log('Offline token detection stopped (stub)');
};

export const isTokenValid = () => {
  return true;
};

export const refreshToken = () => {
  return Promise.resolve();
};