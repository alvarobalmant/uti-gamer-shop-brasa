// Temporary stub for JWT error interceptor
export const setupJWTErrorInterceptor = () => {
  console.log('JWT error interceptor setup (stub)');
};

export const removeJWTErrorInterceptor = () => {
  console.log('JWT error interceptor removed (stub)');
};

export const handleJWTError = (error: any) => {
  console.log('JWT error handled (stub):', error);
};